<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">

    <insert id="insertBatch">
        <!--        insert into dish_flavor (dish_id, name, value)-->
        <!--        values-->
        <!--        <foreach collection="flavors" item="df" separator=",">-->
        <!--            (#{df.dishId},#{df.name},#{df.value})-->
        <!--        </foreach>-->
        insert into setmeal_dish (setmeal_id, dish_id, name, price, copies)
        values
        <foreach collection="setmealDishList" item="sd" separator=",">
            (#{sd.setmealId},#{sd.dishId},#{sd.name},#{sd.price},#{sd.copies})
        </foreach>
    </insert>
    <delete id="deleteBySetmealId">
        delete from setmeal_dish where setmeal_id = #{setmealId}
    </delete>


    <select id="getSetmealIdsByDishIds" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where dish_id
        in
        <foreach collection="dishIds" separator="," open="(" close=")" item="dishId">#{dishId}</foreach>
    </select>
    <select id="getDishBySetmealBy" resultType="com.sky.entity.SetmealDish">
        select *
        from setmeal_dish
        where setmeal_id = #{id}
    </select>
</mapper>
